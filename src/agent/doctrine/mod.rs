//! Embedded doctrine fragments used by the `agent` launcher and the MCP
//! server's `instructions` string. The `.md` files in this directory are
//! checked in and pulled at compile time via `include_str!`.
